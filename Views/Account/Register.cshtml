@{
    ViewData["Title"] = "Cr√©er un compte";
    Layout = "_Layout";
}

<div class="auth-container">
    <div class="auth-card animate__animated animate__fadeIn">
        <div class="text-center mb-3">
            <i class="fas fa-fire-flame-curved" style="font-size: 2.5rem; color: var(--primary-light); filter: drop-shadow(0 0 10px rgba(255,109,0,0.4));"></i>
        </div>
        <h2 class="auth-title">Cr√©er un compte</h2>
        <p class="auth-subtitle">Rejoignez E-FastOrder gratuitement</p>

        <div asp-validation-summary="All" class="text-danger mb-3"></div>

        <form asp-action="Register" method="post">
            <div class="mb-3">
                <label class="form-label"><i class="fas fa-user me-1"></i> Nom complet</label>
                <input type="text" name="nom" class="form-control" placeholder="Votre nom" />
            </div>

            <div class="mb-3">
                <label class="form-label"><i class="fas fa-envelope me-1"></i> Email</label>
                <input type="email" name="email" class="form-control" placeholder="votre@email.com" required />
            </div>

            <div class="mb-3">
                <label class="form-label"><i class="fas fa-lock me-1"></i> Mot de passe</label>
                <input type="password" name="password" class="form-control" placeholder="‚DT¢‚DT¢‚DT¢‚DT¢‚DT¢‚DT¢‚DT¢‚DT¢" required />
                <small class="text-muted">Minimum 4 caract√®res</small>
            </div>

            <div class="mb-4">
                <label class="form-label"><i class="fas fa-lock me-1"></i> Confirmer le mot de passe</label>
                <input type="password" name="confirmPassword" class="form-control" placeholder="‚DT¢‚DT¢‚DT¢‚DT¢‚DT¢‚DT¢‚DT¢‚DT¢" required />
            </div>

            <button type="submit" class="btn-primary-custom w-100 justify-content-center" style="padding: 0.85rem; font-size: 1rem;">
                <i class="fas fa-user-plus"></i> Cr√©er mon compte
            </button>
        </form>

        <div class="text-center mt-4">
            <p class="text-muted">D√©j√† un compte ? <a asp-action="Login" style="color: var(--primary-light); text-decoration: none;">Se connecter</a></p>
        </div>
    </div>
</div>
